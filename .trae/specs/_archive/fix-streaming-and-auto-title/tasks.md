# Tasks

- [x] Task 1: 修复客户端流式输出 — 改用增量追加模式
  - [x] 1.1 修改 ChatPanel.tsx 中 handleStream 回调，将 `setStreamingContent(event.fullContent)` 改为 `setStreamingContent(prev => prev + event.content)` 增量追加
  - [x] 1.2 修改 thinking 内容的流式回调，同样改为增量追加模式
  - [x] 1.3 在流式回调中使用 `flushSync`（从 react-dom 导入）强制 React 立即同步更新DOM，避免批量更新导致视觉跳跃
  - [x] 1.4 确保流式内容初始值为空字符串，首次 content 事件时开始追加

- [x] Task 2: 修复标题自动生成触发条件
  - [x] 2.1 在 ChatPanel.tsx 中修改 `isDefaultTitle` 判断逻辑，增加对自动截取临时标题的识别（标题包含 "..." 或标题等于用户首条消息内容）
  - [x] 2.2 添加 `autoGeneratedTitleRef` 引用，记录自动截取的临时标题文本，用于后续判断
  - [x] 2.3 确保标题自动生成在5秒延迟后正常触发，不受临时标题影响

- [x] Task 3: 检查并修复 Nginx SSE 代理缓冲配置
  - [x] 3.1 检查服务器 Nginx 配置中 AI 相关 location 块是否设置了 `proxy_buffering off`
  - [x] 3.2 如果未设置，添加 `proxy_buffering off` 和 `proxy_cache off` 配置
  - [x] 3.3 重新加载 Nginx 配置

- [x] Task 4: 编译验证与部署
  - [x] 4.1 客户端 TypeScript 编译通过
  - [x] 4.2 上传修改文件到服务器
  - [x] 4.3 服务器重新构建客户端
  - [x] 4.4 重启 PM2 服务并验证

# Task Dependencies

- Task 1 和 Task 2 可并行开发
- Task 3 独立于代码修改，可并行进行
- Task 4 依赖 Task 1、2、3 完成
